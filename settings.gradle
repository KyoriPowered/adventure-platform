rootProject.name = 'adventure-platform-parent'

['common', 'impl-common', 'bukkit', 'bungeecord', 'spongeapi'].each {
  include "platform-$it"
  findProject(":platform-$it")?.name = "adventure-platform-$it"
}
